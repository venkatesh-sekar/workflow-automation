# Flow — Coding Rules

## Repository Identity

- **This is the primary repository.** All edits, changes, and new files go here (`/home/venkatesh/workflow-automation/`).
- **`/primary01/git/activepieces/` is the upstream reference only.** Read from it for reference, but NEVER edit files there.
- When searching for files to modify, always verify the path is within this repo.

## File Structure

- **Exported types and constants must be placed at the end of the file**, after all logic (functions, hooks, components, classes, etc.).
